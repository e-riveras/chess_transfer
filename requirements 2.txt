# chess_transfer - Requirements
# Book to Lichess Study Converter Dependencies

# Core Lichess API
berserk>=0.13.0

# Chess notation parsing and PGN handling
python-chess>=1.9.0

# PDF parsing
PyPDF2>=3.0.0

# EPUB parsing
ebooklib>=0.18

# HTML parsing (for EPUB content extraction)
beautifulsoup4>=4.11.0
lxml>=4.9.0

# Optional: Better PDF text extraction (alternative to PyPDF2)
# Uncomment if PyPDF2 doesn't work well with your PDFs
# pdfplumber>=0.9.0
# pdfminer.six>=20221105

# Development dependencies (optional, but recommended)
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0

# Optional: Streamlit UI (only if you want the web interface)
# streamlit>=1.28.0

# Optional: For better CLI experience
click>=8.1.0
rich>=13.0.0  # Nice terminal output
python-dotenv>=1.0.0  # For .env file support
